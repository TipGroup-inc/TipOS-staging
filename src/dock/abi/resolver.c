/* moe moe kyun <3 */
/* moe moe kyun <3 */
/* ♥ dock ~ gerenciando app/processo, baka fique quieto!
 * arquivo: resolver.c ~ funcoes anotadas: 0
 */
/* ~*~ resolver.c ~*~
 * Hihi, olha esse arquivo aqui~ Que lindo, né? >_<
 * Escrito com muito amor (e gambiarras) pela equipe TipOS!
 * Se quebrar, a culpa é sua~ <3
 *~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*~*/

// ~~ resolver.c ~~
// [TODO] Resolvedor de dependências ABI do Dock.
// Futuramente vai carregar bibliotecas compartilhadas, resolver símbolos,
// e ligar módulos dinamicamente. Por enquanto é um placeholder fofo~ ☆
// Mas quando ficar pronto, vai ser ~lin-do~!

/* ♥ resolver.c ~ feito com carinho (e uma raiva controlada) ~ kyun! */

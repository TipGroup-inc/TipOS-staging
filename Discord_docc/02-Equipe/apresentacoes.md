/* moe moe kyun <3 */
🙋 **Apresente-se!**

Conta pra gente:
- Nome / apelido
- Stack que curte (C, Assembly/NASM, Rust, Python...)
- O que te trouxe pro TipOS e o que quer trabalhar (kernel? drivers? userland? o Doca/HAL?)

Depois de se apresentar, esse canal também é o mural de demos: prints e vídeos do TipOS rodando, `graphy` com syntax highlight, boot no QEMU, resultados de teste em hardware real, etc.

Pra abrir o canal, segue um exemplo do que já roda hoje:

```
MkM> help
MkM> ls
MkM> date
MkM> exec /BIN/GRAPHY
```

Shell com history, autocomplete (TAB), redirecionamento (`>`, `>>`) e PATH search em `/BIN/`, `/USR/BIN/`, `/LOCAL/BIN/` — tudo funcional. 🎉
